import { ScreenScrollView } from '@/src/components/screen-scroll-view';
import Feather from '@expo/vector-icons/Feather';
import { useThemeColor } from 'heroui-native';
import {
  Image,
  Pressable,
  Text,
  useWindowDimensions,
  View,
} from 'react-native';
import Animated from 'react-native-reanimated';
import { withUniwind } from 'uniwind';

const AnimatedPressable = Animated.createAnimatedComponent(Pressable);
const AnimatedImage = Animated.createAnimatedComponent(Image);
const AnimatedView = Animated.createAnimatedComponent(View);

const StyledFeather = withUniwind(Feather);

type HomeCardProps = {
  title: string;
  imageLight: string;
  imageDark: string;
  count: number;
  footer: string;
  path: string;
};

const heroLight =
  'https://heroui-assets.nyc3.cdn.digitaloceanspaces.com/images/heroui-native-example/home-components-light.png';
const heroDark =
  'https://heroui-assets.nyc3.cdn.digitaloceanspaces.com/images/heroui-native-example/home-components-dark.png';

export default function App() {
  // const { isDark } = useAppTheme();
  // const router = useRouter();

  const { height } = useWindowDimensions();
  const themeColorOverlay = useThemeColor('overlay');

  // Press animation styles for CTA buttons
  // const usePressScale = (pressed: boolean) =>
  //   useAnimatedStyle(() => ({
  //     transform: [
  //       {
  //         scale: withTiming(pressed ? 0.98 : 1, {
  //           duration: 120,
  //           easing: Easing.out(Easing.quad),
  //         }),
  //       },
  //     ],

  //   }));

  return (
    // <ScrollShadow
    //   LinearGradientComponent={LinearGradient}
    //   style={{ height: height }}
    //   color={themeColorOverlay}
    // >
    // <ScreenScrollView>
    <ScreenScrollView className="flex-1 bg-background">
      <View
        className="flex-1"
        style={{
          borderWidth: 1,
          borderColor: 'red',
        }}
      >
        <Text
          style={{
            color: 'red',
          }}
        >
          Top Lorem ipsum dolor sit amet, consectetur adipisicing elit.
          Molestiae reprehenderit officiis consequuntur id, voluptates ad minima
          ab blanditiis sapiente architecto eaque quidem sint quasi laborum
          repudiandae animi voluptatum quia maiores. Eum ea sapiente amet
          explicabo iusto sed qui ab a blanditiis. Reiciendis molestiae impedit
          doloremque quod assumenda ex quia inventore beatae! Quo error cumque,
          impedit amet eius repudiandae quia repellat distinctio fugiat, numquam
          tempore maxime officiis quas corrupti eligendi dicta est molestias
          quae dolorem ut quis adipisci, omnis magni. Voluptate voluptas
          reiciendis dignissimos odit porro magni esse dolorem aperiam quod modi
          impedit, quis officiis est accusamus iste nobis repudiandae doloribus?
        </Text>
      </View>
      <View
        className="flex-[0.4]"
        style={{
          borderWidth: 1,
          borderColor: 'red',
        }}
      >
        <Text
          style={{
            color: 'lime',
          }}
        >
          Bottom
        </Text>
      </View>
    </ScreenScrollView>
    // </ScreenScrollView>
    // </ScrollShadow>
  );
}
